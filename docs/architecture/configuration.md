# Configuration

Feature settings are read through the centralized configuration layer.

Important gates include diagnostics and completion.

Disabled features should return no work or clear previous output as appropriate. Configuration changes must not leave stale diagnostics or dependency state.
