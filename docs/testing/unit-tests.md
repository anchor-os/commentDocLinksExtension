# Unit Tests

Cover:

- reference type/status normalization;
- workspace/worktree root selection;
- lexical and symlink escape prevention;
- dependency index add/remove/replace/reverse lookup;
- version freshness;
- scheduler priorities/concurrency/coalescing/failure isolation;
- open and disk-only document scanning;
- target queueing and dependent refresh.
