# Git Worktrees

A document inside a linked Git worktree resolves relative links against that worktree when it is the more specific checkout root.

Tests should cover normal checkout, linked worktree, nested roots, multiple roots, and attempted root escapes.
