# Hover and Decorations

Hover and decoration layers consume the shared reference/resolution model.

Batch decoration processing can memoize filesystem reads so a target is not loaded repeatedly in one pass.

These are presentation layers, not independent parsers or resolvers.
